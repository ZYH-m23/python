#编码相关内容
#1.BPE分词器
#是一种子词分词写法
#用途：将字符串编码为图元
#通俗解读：将文字拆成基础的字节，用BPE算法将其合并成小词块，再给每一个词块分配数字，将文字变为数字，方便计算机使用
#2.the unicode standard
#将字符映射到整数码点（在python中，ord()将字符转化成码点，chr()将码点转化成字符。
#直接使用python提示符，可用于处理一些简单的运算
#eg:>>> a=3
    >>> b=1
    >>> print(a+b)
    4
#3.unicode encodings
#原因：在码点上直接训练不合理，因此使用编码将字符转化成字节序列。
#常见的编码方式：UTF-8是互联网上最主要的编码方式。在Python中， .encode("utf-8")  将字符串转为字节序列，.decode("utf-8")  将字节序列转回字符串。
               将一个庞大（约15万）的码点词汇表转换为了一个非常易于管理、大小仅为256的字节词汇表
#
