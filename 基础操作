字面量
值固定的值

注释
单行：#
多行："""        """

查找数据类型：print(type(变量))

类型转换

int(x)    转换成整数
str(x)    转换成string

运算：注意/是化成float的，而//是取整
转移字符注意：若直接name=""xxx"",会错，但是若用"\"xxx"\",不会错，\就是转义字符
双单引号同上

占位符
eg:message="在%s学习用%s"%(place,name)
%s就是占位符，给string占位。%后面的就是要放进去的变量
%d就是转成整数
%f是整成浮点型

精度控制
%5d是将宽度进行控制在5位
%5.2f是将宽度控制在5，小数点精度控制在2
%。2f限制了精度，不限制宽度，但是有精度限制会四舍五入

字符串格式化方法
print(f"xxx{变量/表达式}")
不理解类型，不对精度有限制

表达式：是一条具有明确执行结果的代码语句
eg:print(f"1*2的结果是{1*2}")


键盘输入：
eg:name=input("输入：")
input默认是字符串结果,若想转化，如下：
name=int(name)
