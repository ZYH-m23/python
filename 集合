集合的定义：
集合是一种无序的，不可重复的，可以修改的数据容器
定义空集合：直接用s2=set(，空集合定义不能用大括号，直接用大括号的是字典
由于集合是无序的，因此是不支持下标索引访问的
s1={1,2,3,5,6,7}
print(s1)没有顺序，自动去重
常用方法
add()  添加元素到集合中
remove()  移除集合中的指定元素（指定元素不存在将报错）
pop()   随即删除集合中的元素并返回
clear()   清空集合
difference()    求两个集合的差（包含在第一个集合但不包含在第二个集合的元素）
way2:直接用 集合1-集合2
way3:s={for s in set1 if s not in set2} 
union()         求取两个集合的并集
intersection()   求取两个集合的交集 ，也可以直接用 名字=集合1&集合二。

格式：集合.方法（）

合并所有：all_set=set1.union(set2).union(set3)...
注意：列表[]可以重复，但是直接用集合不可以

总结：交集：&
    并集：|
     差集：-
变量名称={i 表达式 for i in 列表（if条件）}








