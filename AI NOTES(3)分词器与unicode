#编码相关内容
#1.BPE分词器
#是一种子词分词写法
#用途：将字符串编码为图元
#通俗解读：将文字拆成基础的字节，用BPE算法将其合并成小词块，再给每一个词块分配数字，将文字变为数字，方便计算机使用

#2.the unicode standard
#将字符映射到整数码点（在python中，ord()将字符转化成码点，chr()将码点转化成字符。
#直接使用python提示符，可用于处理一些简单的运算
#eg:>>> a=3
    >>> b=1
    >>> print(a+b)
    4

#3.unicode encodings
#原因：在码点上直接训练不合理，因此使用编码将字符转化成字节序列。
#常见的编码方式：UTF-8是互联网上最主要的编码方式。在Python中， .encode("utf-8")  将字符串转为字节序列，.decode("utf-8")  将字节序列转回字符串。
               将一个庞大（约15万）的码点词汇表转换为了一个非常易于管理、大小仅为256的字节词汇表

#4.子词分词（subword tokenization)
#4.1出现背景：输出序列极长，效率比较低。
#4.2介绍：子词分词是介于词级与字节级之间的部分，从256个基础词汇表开始，
#通过BPE算法将常见的词迭代常见的字节并合成单一的，新的子词，并提取到词汇表中。
#4.3 BPE是一种压缩算法，迭代合并最频繁出现的字节对。通过这种方式训练的词汇表，既能处理OOV，又能有效压缩序列长度。
#    BPE的训练过程：a.词汇表初始化（初始词汇表是256个词汇的字节表。 b.预分词   用正则表达式对语料库进行粗粒度的拆分（如拆分成单词和块），
     BPE的合并不会跨越这些预分词块的边界。
#4.4计算BPE合并（在预分词块内部，迭代执行以下操作）；
#   a.统计所有相邻token对的频率  
#   b.找到频率最高的token 
#   c.用一个新的token“AB”去替换所有的"A"，"B"对 
#   d.将“AB”添加到词汇表里面
#   e.将（“A”，“B”）记录到合并规则列表merges中
#   f.重复此过程，直到达到目标vocab_size
#    平局处理：如果多个字节对频率并列最高，选择字典序最大的一对
#4.5特殊Token
#像<|endoftext|>这样的字符串应该被视为特殊的Token，它们必须被添加词汇表中，并且在任何情况下都不应该被BPE拆分
#4.6 BPE训练的实现与优化
#4.6.1  理论：
#  并行化预分词  可使用multiprocessing库来并行化，在分块的时候，要只在特殊token的边界处拆分，因为BPE合并不会跨越这些边界
#  处理特殊Tokens 在运行正则表达式预分词之前，应先使用re.split按照特殊token处理拆分语料库
#  优化合并步骤  更高效的方法：只增量更新那些与刚合并的token相邻的token对的计数，而不是重新遍历所有的数据
#4.7 BPE分词器（解码与编码）
4.7.1编码文本  
#步骤   a.预分词  使用与**完全相同**的正则表达式，将字符串拆分成预分词块
        b.应用合并  获得BPE训练时生成的merges列表，**按顺序**遍历此列表，将合并规则应用于每个预分词块，直到没有更多可应用的合并。
#解码文本过程
#1.遍历输入token的ID列表
#2.在vocab字典中查找每个ID的bytes
#3.将所有bytes拼接在一起
#4.使用.decode（“utf-8",errors='replace')最终将字节序列转化为字符串
#必须使用errors='replace',这会自动将任何无效的字节序列替换为Unicode替换
字符（U+FFFD)






