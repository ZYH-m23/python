集合
1.	基本格式：s1={ }
2.	特点：可以自动去重，有无序性
3.	添加元素：s2={1,2,3,4}
          print('原集合',s2)
          s2.add(5)
          print('现集合',s2)，一次只能添加一个元素
4.	把传入对象拆分，放入其中：
s2={1,2,3,4}
print('原集合',s2)
s2.update('567')
print('现集合',s2)
5.	删除元素：用s2.remove(X),再进行输出
6.	交集与并集：
交集: print(a&b)
并集：print(a|b)
类型的转换：
Int型：a=1.2
print(a)
b=int(a)
print(b,type(b))    注意：int 的括号里是字符串类型
字符串中有除了数字与正负号以外的就会报错，+-在前面表示正负号，不可在后面
Float型：转化为小数，如果括号内有数字，小数点，正负号之外的字符，不支持转化，括号内是字符串形式。
    Print(float(‘X’))
str():转化为字符串型，任何形式都可以转化，str的意思是字符串
       m=1.8
print(type(m))
st=str(m)
print(type(st))
eval():用来执行字符串表达符并返回表达式，但运算必须是相同类型的。也可以实现list,dict,tuple,str的转换







