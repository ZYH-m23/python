boolean
eg:num1=10
   num2=20
  print(f"10==10的结果是：{num1==num2}")

判断
if 条件：
   statement1.（注意缩进）
注意，涉及到数据类型，用age=int(input("你的年龄是："))
要进行转化
if  condition1:
   statement1
elif condition2:
    statement2
else:
    statement3
（不满足才会逐步向下）
嵌套：
多条件，多层次逻辑判断
注意缩进

eg:import random
num=random.randint(1,10)
print(num)

循环：
while 条件:
 满足条件，statement1
 满足条件，statement2
 满足条件，statement3
  update
当无法预估数的时候，我们要用true和false进行控制，比如while true:
eg:
import random
r=random.randint(1,10)
count=0
has=True
while has:
    num=input("Enter a number:")
    count+=1      #更新状态
    if num==r:
        print("Correct")
        has=False     #注意更新状态
    else:
        print("Incorrect")
print(count)

\t对齐符（多行）
eg:
i=1
while i<=9:
    j=1
    while j<=i:
        print(f"{i}*{j}={i*j}\t",end='')     #对齐，以及空格
        j=j+1
    i+=1
    print()    # 内层循环结束后换行

for循环
轮询机制，对内容进行逐个处理
for 变量 in 集合：
   statement
  无法定义循环条件，只能被动取出数据处理
  不能构建无限循环

range(a,b,c):
范围处理
每一个间隔是c
从a开始，从b结束，但不包括b

内容不同且有恒定变化，就用嵌套

continue        终止本次循环，直接进入下一次循环
break     整体全部over
